/**
 * This package contains annotations that are used to specify scope of lifetime of server objects.<br>
 * (for example, should always new instance of class X be created in the transaction or always be the same instance 
 *  in the same session but different in different sessions etc.)
 */
package org.mozzes.application.module.scope;